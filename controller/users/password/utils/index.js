const { invalidate } = require('./invalidate'),
  { increment } = require('./increment'),
  { validate } = require('./validate');

module.exports = {
  invalidate,
  increment,
  validate,
};
