const { New, Update, Receipt } = require('./celebrations');
const { Test } = require('./Test');
const { ContributingInquiry } = require('./ContributingInquiry');

module.exports = { New, Update, Receipt, Test, ContributingInquiry };
