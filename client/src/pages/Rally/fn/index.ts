/**
 * Rally page pure helpers (share copy URL and suggested message text).
 * @module pages/Rally/fn
 */

export {
  buildSuggestedShareMessage,
  resolveRallyShareUrl,
} from './buildSuggestedShareMessage';
export { getRallySiteUrl } from './getSiteUrl';
